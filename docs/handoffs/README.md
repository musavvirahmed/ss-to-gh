# Handoffs

Session-bridge notes so another IDE or model can continue.

`/handoff` in this repo writes here (not only to the OS temp dir). Name files `YYYY-MM-DD-<topic>.md`. Point at the Wayfinder map and tickets; do not restate decisions that already live on issues.
